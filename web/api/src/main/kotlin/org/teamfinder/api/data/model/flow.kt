package org.teamfinder.api.data.model

//fun Flow<EventRegistration>.filterWithSearch(filters: EventRegistrationFilters?): Flow<EventRegistration> {
//    if (filters == null) return this
//
//    return filter {
//        (filters.eventId == null || filters.eventId == it.pk.eventId) &&
//                (filters.subscriptionId == null || filters.subscriptionId == it.subscriptionId) &&
//                (filters.visitorId == null || filters.visitorId == it.pk.visitorId) &&
//                (filters.ownerId == null || filters.ownerId == it.ownerId)
//    }
//}
